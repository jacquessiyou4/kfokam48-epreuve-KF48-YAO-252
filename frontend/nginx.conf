server {
    listen 80;
    root /usr/share/nginx/html;

    location /api/ {
        proxy_pass http://backend:8080;
    }

    # Routes React (formateur, etudiant, relecteur) servies par index.html
    location / {
        try_files $uri /index.html;
    }
}
